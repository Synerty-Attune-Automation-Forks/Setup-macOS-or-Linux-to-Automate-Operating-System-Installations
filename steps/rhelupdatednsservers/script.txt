ETHERNET_DEVICE=$(nmcli device status | grep ethernet | awk '{print $1}')
echo "ETHERNET_DEVICE=[${ETHERNET_DEVICE}]"

nmcli con mod ${ETHERNET_DEVICE} ipv4.dns "{attuneSubnet.dns1} {attuneSubnet.dns2}"
nmcli con mod ${ETHERNET_DEVICE} ipv4.dns-search {adFullDomainName.value}
nmcli con up ${ETHERNET_DEVICE}